# Document template registration — Phase 2.
#
# Registered in Foundry IQ `kb-document-templates` so agents can reference
# this document type from Phase 3 onward. The full field map, preflight
# rules, and state variations are built out in Phase 5
# (backend/documents/template_engine.py).

document_type: motion_default_judgment
display_name: Motion for Default Judgment
description: >
  Motion filed when the defendant fails to respond to the petition by the
  court deadline. Generation is hard-gated until the response deadline has
  passed.
applies_to_dispute_types:
  - security_deposit
generates_with: []
status: stub
fields: []
state_variations: {}
